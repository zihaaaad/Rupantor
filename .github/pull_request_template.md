## What this changes

<!-- A sentence or two. If it fixes an issue or an AUDIT.md finding, link it. -->

## Why

<!-- The problem being solved, not just the mechanics of the change. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npx tsc -b` passes
- [ ] `npm test` passes
- [ ] `npx vite build` passes
- [ ] If this fixes a bug, a test covers it (and fails without the fix)
- [ ] If this touches IPC, shell/PowerShell/AppleScript, or file paths, I have read the non-negotiable rules in CONTRIBUTING.md

## Tested on

<!-- e.g. Windows 11, Photoshop 2025 — the native paths differ per platform. -->
